# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtualenv
.venv/
venv/
ENV/
env/

# IDE / editors
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project specific
.env
.env.local
.env.example.local
piherder_backups/
Caddyfile.old/
*.log
*.pid
*.sqlite
backups/   # local testing of backup volume
alembic/versions/*.py  # keep initial only or versioned

# Docker
docker-compose.override.yml
*.env

# SSH temp keys during jobs (should never persist)
tmp_ssh_key_*
*.pem
id_rsa*

# Alembic
alembic/versions/__pycache__/

# Vendored frontend CDNs (generated by scripts/vendor_cdns.sh for offline/air-gapped support)
# These are downloaded at build/dev time so the container works with zero internet.
app/static/tailwind.js
app/static/htmx.min.js
app/static/alpine.min.js

# App data (avatars)
piherder_data/
